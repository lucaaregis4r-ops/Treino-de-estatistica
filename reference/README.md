# Referência StatsBomb offline

Gerada por `node reference/generate-statsbomb-reference.mjs` a partir da fixture pequena pinada de `match_id=15946`. A fixture é um recorte de teste atribuído ao repositório `hudl/open-data`, não uma distribuição dos eventos completos. O arquivo derivado registra fonte, cobertura, regras, `alpha=10` e SHA da fixture. Não há download nem acesso de rede durante o jogo.
