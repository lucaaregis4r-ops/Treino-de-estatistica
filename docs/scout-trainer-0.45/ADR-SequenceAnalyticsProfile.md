# ADR — SequenceAnalyticsProfile (M6)

Status: contrato futuro, não implementado.

O perfil futuro deve versionar estados canônicos pequenos, janela de padrão, unidade (`rally`/`event`) e política de ranking. Nesta etapa o contrato é somente para voleibol; o resultado é calculado sob demanda e não é salvo em `ScoutEvent`.

Futebol, futsal e basquete ficam fora do escopo deste pacote e exigem ADR própria.
